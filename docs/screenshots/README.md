# Screenshots

Canonical v0.1 README screenshots:

- `main_popover.png`: GNOME top-bar popover with the primary provider usage
  view.
- `settings1.png`: settings view for daemon and refresh controls.
- `settings2.png`: settings view for provider controls.

Keep these files small enough for the repository and update `README.md` when
replacing them.
