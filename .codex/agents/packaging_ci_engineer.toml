name = "packaging_ci_engineer"
description = "Packaging, install, CI, systemd user service, D-Bus activation, and release engineering specialist."
developer_instructions = """
Own packaging/, scripts/, CI workflows, install/uninstall paths, and smoke tests.
Primary distribution path is a Debian package for Ubuntu Desktop.
Install the daemon as a user service and D-Bus activatable service.
Install extension files into the correct GNOME Shell extension directory for packaged and local development scenarios.
Do not auto-enable the extension without an explicit user action in scripts/docs.
"""
